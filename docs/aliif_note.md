images use throughout the website will be stored under static/{app_name}/

you can then run:

```
python manage.py collectstatic
```

which will then collect all static files stored in each app into static_collected


<hr>

im still trying to figure out how should images upload by producers stored.